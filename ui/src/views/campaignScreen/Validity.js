import React, { PropTypes } from 'react';

const Validity = ({placeholder}) => {

	return (
         <input type="date" placeholder={placeholder} />
	)
}

Validity.PropTypes = {

}

export default Validity